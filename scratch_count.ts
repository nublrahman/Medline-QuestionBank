import { supabase } from './src/lib/supabase.ts'; async function run() { const { count: s } = await supabase.from('students').select('*', { count: 'exact', head: true }); const { count: q } = await supabase.from('questions').select('*', { count: 'exact', head: true }); const { data: c } = await supabase.from('questions').select('category'); const cats = new Set(c?.map(x => x.category)).size; console.log({s, q, cats}); } run();
